mutation GetUserInfo($token: String!) {
    loginByGoogle(token: $token) {
        accessToken
        isNew
        message
        user {
            avatar
            email
            fcm_id
            fullName
        }
    }
}

